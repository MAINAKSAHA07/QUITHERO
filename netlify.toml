[build]
  command = "npm run build"
  publish = "dist"
  base = "."

[build.environment]
  NODE_VERSION = "18"

# PocketBase proxy + SPA rules are generated into dist/_redirects at build time
# (see scripts/write-netlify-redirects.js). Set VITE_POCKETBASE_URL in Netlify env.
